/*
1 FUNCAO PRA CALCULAR A SOMA E A OUTRA PRA CALCULAR A MEDIA
*/
#include <iostream>

using namespace std;

int le_calcula_soma(int x, int y)
{
int soma;
cin >> x;
cin >> y;
soma = x + y;

return soma;
}
int calcula_media(int soma)
{
int media, x, y;
soma = x + y;
media = soma/2;

return media;
}